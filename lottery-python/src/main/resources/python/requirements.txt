# 双色球多层融合模型 —— 源码实际依赖
# 推荐 Python 3.9 ~ 3.12（Java 端调用命令为 `python`，须在 PATH 中）
# 一键安装：双击项目根目录 install_python_deps.bat
#          或本目录 python install_deps.py

# ---- 必需（train_models / predict / backtest / ssq_features）----
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
xgboost>=1.5.0

# 深度学习（缺失时训练脚本会自动跳过对应模型，不阻断流程）
# LSTM: from tensorflow import keras
# Transformer: torch / torch.nn / torch.optim / DataLoader
tensorflow>=2.8.0
torch>=1.10.0
